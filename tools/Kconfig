menu "Tools (Filesystem) Config"
    comment "Linux targets use the local filesystem (tal_fs). MCU-only options"
    comment "(SD card, hardware MCP, embedded Lua) are not available in this fork."
endmenu
